export enum RoomStatus {
    Lobby = 'lobby',
    Starting = 'starting',
    Playing = 'playing',
    Finished = 'finished'
}

export enum PlaylistType {
    Spotify = 'spotify',
}

export enum RoomGuessMode {
    Song = 'song',
    Artist = 'artist',
    Both = 'both'
}

export enum RoundType {
    Song = 'song',
    Artist = 'artist',
}